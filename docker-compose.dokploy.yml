version: '3.8'

services:
  inventario-sistemas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inventario_sistemas_app
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PORT=3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
